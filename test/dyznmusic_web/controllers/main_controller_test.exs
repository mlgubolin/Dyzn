defmodule DyznMusicWeb.MainControllerTest do
  use DyznMusicWeb.ConnCase

  # test "GET /", %{conn: conn} do
  #   conn = get(conn, "/rest")
  #   assert html_response(conn, 200) =~ "Welcome to Phoenix!"
  # end
end
